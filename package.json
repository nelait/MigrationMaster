{
  "name": "maigration-master",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup": "npm run install:all && cd server && npx prisma generate && npx prisma db push && node seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
